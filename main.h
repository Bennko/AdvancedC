#ifndef MAIN_H
#define MAIN_H

#define MAX_COMMAND_LENGTH 6
#define MAX_NAME_LENGTH (20 + 1)
#define MINIMUM_PARTITION_SIZE 500 //without adding anyting the size of partition is already around 173 currently
#define MAXIMUM_PARTITION_SIZE 4294967295 //we use unsigned int for length

#endif